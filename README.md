Install the RubyInstall on windows
```sh
http://rubyinstaller.org/
```

Install the compass
```sh
gem install compass
```

Install the gem
```sh
gem install bootstrap-sass
```

If you have an existing Compass project:

```ruby
# config.rb:
require 'bootstrap-sass'
```

Compile Sass to CSS
```sh
compass watch
```
